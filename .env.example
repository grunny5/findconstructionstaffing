# Supabase Configuration
# Get these values from your Supabase project dashboard

# Your Supabase project URL (found in Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Your Supabase anon/public key (found in Settings > API)
# This key is safe to use in browser code
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Supabase project reference (extracted from URL)
# Used by CLI tools for direct database operations
# SUPABASE_PROJECT_REF=your-project-ref

# Optional: Service role key for server-side operations (keep this secret!)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional: Supabase Dashboard URL for migration helper script
# SUPABASE_PROJECT_URL=https://supabase.com/dashboard/project/your-project-id

# Security Configuration
# Salt for hashing IP addresses (generate a random string for production)
# This ensures IP addresses are never stored in plain text
IP_HASH_SALT=your-random-salt-here-change-in-production

# Resend Email Service Configuration
# Webhook secret for verifying Resend webhook events
# Get this from Resend Dashboard > Webhooks > Your webhook > Signing Secret
# Format: whsec_xxxxxxxxxxxxxxxxxxxxx
RESEND_WEBHOOK_SECRET=whsec_your-webhook-secret-here

# Upstash Redis Configuration (Required for Production)
# Used for persistent rate limiting across serverless function invocations
# Get these values from: https://console.upstash.com/
# Create a Redis database, then go to Details > REST API
UPSTASH_REDIS_REST_URL=https://your-redis-name.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-token-here

# Feature Flags
# Control rollout of authentication features without code deployments
# Accepted values: 'true', '1', 'yes', 'enabled' (case-insensitive) = enabled
# Any other value or omitted = disabled (safe default)

# Enable email verification during signup
# When enabled: Users must verify email before login
# When disabled: Users can login immediately after signup
NEXT_PUBLIC_FEATURE_EMAIL_VERIFICATION=false

# Enable password reset functionality
# When enabled: "Forgot password" link shown on login page
# When disabled: Password reset UI hidden
NEXT_PUBLIC_FEATURE_PASSWORD_RESET=false

# Enable account settings page
# When enabled: Users can access /settings to manage profile, email, password
# When disabled: Settings navigation hidden
NEXT_PUBLIC_FEATURE_ACCOUNT_SETTINGS=false

# Enable admin dashboard
# When enabled: Admin users can access /admin/users for role management
# When disabled: Admin UI hidden (role changes require database access)
NEXT_PUBLIC_FEATURE_ADMIN_DASHBOARD=false

# Enable resend verification email
# When enabled: Users can request new verification emails
# When disabled: Resend UI hidden
NEXT_PUBLIC_FEATURE_RESEND_VERIFICATION=false