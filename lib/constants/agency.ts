/**
 * Agency-related constants
 */

/**
 * Maximum attempts to find a unique slug before failing.
 * Used when generating slugs for new agencies to prevent infinite loops.
 */
export const MAX_SLUG_ATTEMPTS = 10;
